import React from "react";
// Component for the icons for the sunset and sunrise
const MainIcon = () => {
    return (
        <div className='flex-rowpictures'>
            {/* Displays images using css */}
            <div className='sunset' /> 
            <div className='sunrise' />
          </div>
    );
};

export default MainIcon; 
